/// <reference types="vite/client" />

declare const __AO_ENABLE_TEST_SURFACES__: boolean;

/** Commit + build time of this bundle, or "dev" when served by the dev server. */
declare const __AO_BUILD_ID__: string;
