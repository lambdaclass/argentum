[package]
name = "ao-topology"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Reads the world corpus and reports what its exits imply about geography."

[dependencies]
ao-core = { path = "../ao-core" }
