[package]
name = "ao-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Deterministic gameplay rules shared by the Argentum client and server."

# No dependencies on purpose. This crate must compile for wasm32 (client),
# native (tests) and eventually as part of the Rustler NIF (server), so it
# stays free of platform and framework coupling.
[dependencies]
